# Legacy Templates

> ⚠️ **This folder is for reference only.**

The **active templates** used by `pulse init` are located in:

```
packages/pulse-cli/templates/
```

## Why this folder exists

This folder contains older versions of the templates for historical reference.
When contributing or modifying templates, please edit the files in `packages/pulse-cli/templates/`.

## Active Template Locations

| Template | Location |
|----------|----------|
| `.cursorrules` | `packages/pulse-cli/templates/.cursorrules` |
| `AGENTS.md` | `packages/pulse-cli/templates/AGENTS.md` |
| `pulse.mdc` | `packages/pulse-cli/templates/cursor/pulse.mdc` |
| Role templates | `packages/pulse-cli/templates/roles/` |
